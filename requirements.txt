# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Image processing
pillow>=10.0.0
numpy>=1.26.0
opencv-python>=4.9.0

# Apple frameworks via PyObjC
pyobjc-framework-Vision>=10.0
pyobjc-framework-Quartz>=10.0
pyobjc-framework-CoreML>=10.0

# MLX for CLIP (Apple Silicon optimized)
# Requires macOS >= 14.0, Python >= 3.10
# 0.31.2 crashes with "no Stream(gpu, 0)" in thread pool (#38)
mlx>=0.22.0,<0.31.2
huggingface-hub>=0.20.0

# Pin mlx-clip to specific commit for stability
mlx-clip @ git+https://github.com/harperreed/mlx_clip.git@f56e3ecc72c74c68b6b50eb6f50c3f22fc23fe2c

# Native MLX SigLIP2 backend (Blaizzy/mlx-embeddings). Drives the
# ViT-SO400M-16-SigLIP2-384__webli smart-search model on-device via MLX.
# Pinned: the loader's patch-size regex and the get_image_features/
# get_text_features API are version-specific (see ml-ycd.1 spike).
# Pulls in mlx-vlm + transformers.
mlx-embeddings==0.1.0

# NOTE: the open_clip/MPS fallback was removed in ml-b82. open_clip's bundled
# SigLIP transform squashes (resize-to-square) instead of Immich's resize-
# shortest+center-crop, so it produced index-incompatible embeddings (~0.83
# cosine) — the one path that broke parity with the upstream Immich ML server.
# Models are served only by the native MLX SigLIP2 backend above or mlx-clip;
# any other model raises in clip.py:_load_model. (torch is still pulled in by
# mlx-clip.) The scripts/embedding_parity.py 'openclip' diagnostic optionally
# wants open-clip-torch; install it manually if you need that reference.

# Face recognition - official onnxruntime supports Apple Silicon via CoreML
# onnxruntime 1.18+ supports numpy 2.x
onnxruntime>=1.18.0
# onnx proto is used directly to read ArcFace model shapes without an
# InferenceSession (also a transitive insightface dep; pinned explicitly)
onnx>=1.16.0
insightface>=0.7.3

# Development/testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0