# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Image processing
pillow>=10.0.0
numpy>=1.26.0
opencv-python>=4.9.0

# Apple frameworks via PyObjC
pyobjc-framework-Vision>=10.0
pyobjc-framework-Quartz>=10.0
pyobjc-framework-CoreML>=10.0

# MLX for CLIP (Apple Silicon optimized)
# Requires macOS >= 14.0, Python >= 3.10
# 0.31.2 crashes with "no Stream(gpu, 0)" in thread pool (#38)
mlx>=0.22.0,<0.31.2
huggingface-hub>=0.20.0

# The OpenAI-CLIP backend (ViT-B-32/B-16/L-14 __openai) is vendored in-tree at
# src/models/clip_mlx.py — a port of Apple's mlx-examples CLIP, formerly the
# dormant third-party mlx-clip package. The vendoring fixed the parity bug where
# that port hardcoded quick_gelu: Immich's ONNX export runs standard gelu, so the
# vendored backend matches the index by running gelu (see clip_mlx.py).
# The vendored BPE tokenizer needs `regex` directly (not just transitively).
regex>=2024.0.0
# torch is an OPTIONAL, convert-only dependency and is intentionally NOT installed
# by default — it's large, and the default SigLIP2 smart-search path never needs
# it. torch is required ONLY for the one-time HF->MLX weight conversion of the
# OpenAI CLIP ports (ViT-B-32/B-16/L-14 __openai; the source pytorch_model.bin is
# a torch pickle); serving already-converted weights needs only mlx. Requesting an
# OpenAI CLIP model on a fresh, torch-free install raises an actionable error (not
# a bare ImportError) from clip_mlx.convert_weights telling you to install it:
#   pip install torch>=2.2.0

# Native MLX SigLIP2 backend (Blaizzy/mlx-embeddings). Drives the
# ViT-SO400M-16-SigLIP2-384__webli smart-search model on-device via MLX.
# Pinned: the loader's patch-size regex and the get_image_features/
# get_text_features API are version-specific.
# Pulls in mlx-vlm + transformers.
mlx-embeddings==0.1.0

# CLIP models are served only by the native MLX SigLIP2 backend above or the
# vendored OpenAI-CLIP backend (src/models/clip_mlx.py); any other model raises in
# clip.py:_load_model. scripts/embedding_parity.py has an optional 'openclip'
# diagnostic backend — `pip install open-clip-torch` manually if you want to run it.

# Face recognition - official onnxruntime supports Apple Silicon via CoreML
# onnxruntime 1.18+ supports numpy 2.x
onnxruntime>=1.18.0
# onnx proto is used directly to read ArcFace model shapes without an
# InferenceSession (also a transitive insightface dep; pinned explicitly)
onnx>=1.16.0
insightface>=0.7.3
# Used directly by face_embed._norm_crop for the 5-point ArcFace similarity
# transform (SimilarityTransform.from_estimate). Also a transitive insightface
# dep, but we import it ourselves now, so pin it explicitly. >=0.26 is where the
# from_estimate constructor exists (and the in-place estimate() was deprecated).
scikit-image>=0.26

# Development/testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0