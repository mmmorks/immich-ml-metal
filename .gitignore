# Python
**/__pycache__/
*.py[cod]
.pytest_cache/

# Virtualenv
venv/
.venv/

# Downloaded model weights / runtime caches
# Anchored to the repo root so src/models/ (source code) is NOT ignored.
/mlx-community/
/models/
/cache/
